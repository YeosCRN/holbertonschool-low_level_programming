include <stdio.h>

/**
 * main - prints a sentence ending with new line
 * Return: value must end in 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
